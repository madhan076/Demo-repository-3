#include<stdio.h>
int main()
{
	int arr[],size;
	printf("Enter size of array:");
	scanf("%d",&size);
	int findmin[],int findmax[];
	for(int i=0;i<size;i++);
	{
		if(arr[i]<size);
		{
			printf("minimum element:%d",findmin);
		}
	}
	for(i=0;i<size;i++)
	{
		if(arr[i]>size)
		{
			printf("maximum element:%d",findmax);
		}
	}
}
